//NOTE Generated by C:\Users\vodan\4ed\code\meta\meta_main.cpp:284:
//NOTE source: C:\Users\vodan\4ed\code\custom_command_list.h

function void
initialize_managed_id_metadata(App *app)
{
#define X(name, group) name = managed_id_declare(app, strlit(#group), strlit(#name))
X(defcolor_bar, colors);
X(defcolor_base, colors);
X(defcolor_pop1, colors);
X(defcolor_pop2, colors);
X(defcolor_back, colors);
X(defcolor_margin, colors);
X(defcolor_margin_hover, colors);
X(defcolor_margin_active, colors);
X(defcolor_list_item, colors);
X(defcolor_list_item_hover, colors);
X(defcolor_list_item_active, colors);
X(defcolor_cursor, colors);
X(defcolor_at_cursor, colors);
X(defcolor_highlight_cursor_line, colors);
X(defcolor_highlight, colors);
X(defcolor_at_highlight, colors);
X(defcolor_mark, colors);
X(defcolor_text_default, colors);
X(defcolor_comment, colors);
X(defcolor_comment_pop, colors);
X(defcolor_keyword, colors);
X(defcolor_str_constant, colors);
X(defcolor_char_constant, colors);
X(defcolor_int_constant, colors);
X(defcolor_float_constant, colors);
X(defcolor_bool_constant, colors);
X(defcolor_preproc, colors);
X(defcolor_include, colors);
X(defcolor_special_character, colors);
X(defcolor_ghost_character, colors);
X(defcolor_highlight_junk, colors);
X(defcolor_highlight_white, colors);
X(defcolor_paste, colors);
X(defcolor_undo, colors);
X(defcolor_back_cycle, colors);
X(defcolor_text_cycle, colors);
X(defcolor_line_numbers_back, colors);
X(defcolor_line_numbers_text, colors);
X(view_rewrite_loc, attachment);
X(view_next_rewrite_loc, attachment);
X(view_paste_index_loc, attachment);
X(view_is_passive_loc, attachment);
X(view_snap_mark_to_cursor, attachment);
X(view_ui_data, attachment);
X(view_highlight_range, attachment);
X(view_highlight_buffer, attachment);
X(view_render_hook, attachment);
X(view_word_complete_menu, attachment);
X(view_lister_loc, attachment);
X(view_previous_buffer, attachment);
X(buffer_map_id, attachment);
X(buffer_eol_setting, attachment);
X(buffer_lex_task, attachment);
X(sticky_jump_positions_handle, attachment);
X(attachment_tokens, attachment);
X(defcolor_vim_filebar_pop, colors);
X(defcolor_vim_chord_text, colors);
X(defcolor_vim_chord_unresolved, colors);
X(defcolor_vim_chord_error, colors);
X(vim_buffer_prev_visual, attachment);
X(vim_buffer_marks, attachment);
X(vim_view_jumps, attachment);
X(defcolor_function, colors);
X(defcolor_type, colors);
X(defcolor_primitive, colors);
X(defcolor_macro, colors);
X(defcolor_control, colors);
X(defcolor_struct, colors);
X(defcolor_non_text, colors);
X(defcolor_broken_link, colors);
#undef X

}